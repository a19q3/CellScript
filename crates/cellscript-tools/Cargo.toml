[package]
name = "cellscript-tools"
version = "0.25.0"
edition = "2024"
rust-version = "1.97.1"
publish = false
description = "Release, audit, and validation tooling for the CellScript workspace"
license = "MIT"

[[bin]]
name = "cellscript-tools"
path = "src/main.rs"

[dependencies]
anyhow = "1.0"
cellscript = { path = "../..", default-features = false, features = ["wasm"] }
blake2b-ref = "0.3"
ckb-jsonrpc-types = "1.0.0"
ckb-types = "1.0.0"
clap = { version = "=4.5.49", features = ["derive"] }
hex = "0.4"
hex-literal = "0.4"
k256 = { version = "0.13.4", default-features = false, features = ["schnorr"] }
percent-encoding = "2"
regex = "1"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "rustls-tls"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
time = { version = "0.3", features = ["formatting", "local-offset"] }
toml = "0.8"
wait-timeout = "0.2"
